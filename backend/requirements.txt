# AquaRoute backend dependencies.
# Version floors chosen for Python 3.11–3.13 compatibility. Pin exact versions
# in a lockfile for a paper-grade reproduction.

# --- Core / config ---
pyyaml>=6.0
python-dotenv>=1.0
pydantic>=2.6
pydantic-settings>=2.2
requests>=2.31

# --- API ---
fastapi>=0.110
uvicorn[standard]>=0.29

# --- Database (PostGIS) ---
SQLAlchemy>=2.0
GeoAlchemy2>=0.15
psycopg[binary]>=3.1

# --- Data / geospatial (Phase 1–2) ---
numpy>=1.26
pandas>=2.2
xarray>=2024.3
osmnx>=1.9
networkx>=3.2
geopandas>=0.14
shapely>=2.0
pyproj>=3.6
rtree>=1.2
rasterio>=1.3
rioxarray>=0.15
pyarrow>=15.0            # GeoParquet segment store (fast reads)

# --- DEM hydrology (Phase 2) ---
pysheds>=0.3
richdem>=2.3; platform_system != "Windows"   # optional; wheels flaky on Windows

# --- Ground truth (Phase 3): real Sentinel-1 SAR flood masks ---
# Planetary Computer path is keyless (anonymous signed reads); GEE needs an account.
pystac-client>=0.7
planetary-computer>=1.0
odc-stac>=0.3
rioxarray>=0.15
scipy>=1.11                # speckle filter / morphology for SAR change detection
earthengine-api>=0.1.390   # optional GEE fallback (needs `earthengine authenticate`)

# --- Baseline + core model (Phase 4–5) ---
scikit-learn>=1.4
xgboost>=2.0
# For the prototype install the CPU build first to avoid a large CUDA download:
#   pip install torch --index-url https://download.pytorch.org/whl/cpu
torch>=2.2
torch-geometric>=2.5

# --- Self-calibration (Phase 7) ---
ruptures>=1.1
river>=0.21

# --- Routing (Phase 8) ---
openrouteservice>=2.3

# --- Scheduler / loop ---
APScheduler>=3.10

# --- Dev / test ---
pytest>=8.0
matplotlib>=3.8
